/*
  g.beginFill(0xFF0000);
  g.drawCircle(100, 200, 50);
*/


//  var i = 1;
//  p.graphics.drawCircle( 40 + i *20, 40 + i * 20, 10 );

/*
  g.moveTo(400, 400);
  g.lineStyle(3, 0x0000FF);
  g.lineTo(200, 200);
  g.drawCircle(200, 300, 50);
  Shapes.drawTriangleLeft(g, 50);
*/


//trace("Number of elements " + polys.length + ", " + polys[1].length);
//  Shapes.drawPolygon(g, polys[0]);                

/*
  var pp = polys[0];
  var i : int;
  g.moveTo(pp[0], pp[1]);
  for(i = 2; i < pp.length; i+=2)
     g.lineTo(pp[i], pp[i+1]);
*/

// g.endFill();

//  ref.addChild(p);